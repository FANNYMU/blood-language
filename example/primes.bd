fn is_prime(n) do
    if n < 2 then
        return false
    end
    if n == 2 then
        return true
    end

    let mod i = 2
    while i * i <= n do
        if n % i == 0 then
            return false
        end
        i = i + 1
    end

    return true
end

fn print_primes(count) do
    let mod found = 0
    let mod n = 2

    while found < count do
        if is_prime(n) then
            print(n)
            found = found + 1
        end
        n = n + 1
    end
end

print(123456789)

print_primes(10)
